# URL of your backend server API (leave blank in dev to use Vite local proxy)
# Example for separate deployment (e.g. Render / Railway / Heroku):
# VITE_API_BASE_URL=https://your-solver-api.onrender.com
VITE_API_BASE_URL=
